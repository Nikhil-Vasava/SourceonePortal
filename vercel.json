{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "prisma generate && next build",

  "regions": ["syd1"],

  "functions": {
    "app/**": {
      "maxDuration": 60
    }
  }
}
